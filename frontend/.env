VITE_BACKEND_BASE_URL=http://localhost:8000
# docker build \
#   -t sreeharij/blogspace-frontend:latest \                                 ----> since vite env file are only read during buid time
#   --build-arg VITE_BACKEND_BASE_URL=http://blogspace-backend-service:8000 .
